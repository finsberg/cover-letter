%!TEX program = xelatex 

\documentclass[$size$]{letter}
\usepackage[letterpaper,margin=1in]{geometry}
\usepackage{graphicx}

\usepackage{fixltx2e}

% Adds microtypography features: character protrusion, for XeTeX.
\usepackage{microtype}

% Allows links.
\usepackage[usenames,dvipsnames]{color}
\usepackage[xetex, bookmarks, colorlinks, breaklinks]{hyperref}
\hypersetup{colorlinks=true, urlcolor=NavyBlue, linkcolor=NavyBlue}

\urlstyle{rm}

\frenchspacing{}        % Remove extra spaces after periods.
\pagestyle{empty}       % Remove page numbers.

\usepackage{fontspec}
\defaultfontfeatures{Mapping=tex-text}  % Sets quotation marks.
\setromanfont{Times}
\setsansfont[BoldFont={Helvetica Neue Bold}]{Helvetica Neue}
\setmonofont[Scale=MatchLowercase]{Helvetica}

% From default template
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

\newcommand\shyp{\-}

\newcommand{\header}[9]{
    {\hypersetup{hidelinks}
    \noindent
    \parbox[b][0.625in][t]{6.5in}{
        \vspace{-0.375in}
        \parbox[b][0.625in][t]{3.74in}
            {\href{http://www.simula.no}
            {\includegraphics[width=1in]{./simula.pdf}}}\hfill
        \parbox[b][0.625in][t]{2in}{
            \setlength{\baselineskip}{9pt}
            \fontsize{7pt}{7pt}{
                \fontspec{Helvetica}
                \textbf{#7}\\
                {#8}\\
                {#9}
                \vspace{6pt}\\
                #1\\
                #2 #3, #4\\
                Phone: #5\\
                Email: \href{mailto:#6}{#6}
                }
            }
        }}
    }

$if(draft)$
\usepackage[nostamp]{draftwatermark}
\SetWatermarkLightness{0.9}
$endif$

\begin{document}

\header{$head.address$}{$head.zip$}{$head.city$}{$head.country$}{$head.phone$}{$head.email$}{$sig.name$}{$head.dept$}{$head.affiliation$}


\vspace{0.5in}

\today

\vspace{0.5in}

\underline{\textbf{\LARGE $title$}}\\

$body$

$if(sig.include)$
    \begin{minipage}[t]{6.5in}
    Sincerely,\\
    
    \vspace{0.1in}

    $if(sig.sign)$
        \vspace{-0.2in}\includegraphics[height=1.2in]{./Signature.pdf}
    $else$
        \\
    $endif$
    \\
    $sig.name$\\
    $sig.post$
    \end{minipage}
$endif$

$if(cc)$
    cc: $for(cc)$$cc$$sep$, $endfor$
$endif$

$if(ps)$
    $ps$
$endif$

\end{document}